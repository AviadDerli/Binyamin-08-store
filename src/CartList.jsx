import CartItem from "./CartItem";

export default function CartList({ cart, setCart }) {

    console.log("cart:" ,cart)
    return (
        <div>CartList

            {/* {.map(c => <CartItem cart={cart} setCart={setCart} />)} */}
        </div>
    )
}
